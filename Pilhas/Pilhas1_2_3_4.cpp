#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */


1 - Pilhas são estruturas de dados que armazenam os elementos em um formato sequencial, 
empilhando um item acima do outro. Elas podem ser utilizadas em situações de desfazer ou refazer em editores de texto
navegações entre páginas na web e também em funções recursivas em compiladores.


2- Uma alocação sequencial para um conjunto de elementos, significa alocar a memória de forma contígua/adjacente entre seus intervalos

3 - Na alocação estática o espaço de memória, que as
variáveis irão utilizar durante a execução do programa, é
definido no processo de compilação. Não sendo possível
alterar o tamanho desse espaço durante a execução do
programa. 


4 - Na alocação dinâmica o espaço de memória, que as
variáveis irão utilizar durante a execução do programa, é
definido enquanto o programa está em execução.
	

